import type { TrackValue } from "../../hooks/track";

export type _Props = {
  isTrackRoute: boolean;
  responsiveCol: string;
  track: TrackValue;
  src: string;
  tapeTitle: string;
  tapePath: string;
};
